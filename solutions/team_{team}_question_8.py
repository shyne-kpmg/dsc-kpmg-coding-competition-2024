from __future__ import annotations

xy_coord = tuple[float, float]  # You don't have to worry about what this means

def Solution(
    # `points` will be a list of tuples containing an x and a y value
    points: list[xy_coord],
    center_x: float,
    center_y: float,
    radius: float,
) -> int:
    return ...